syntax = "proto3;

package = "queue";

service Queue {
    rpc Create (Response) returns (Response);
}

message Response{
    bool success = 1;
    string message = 2;
    oneof data {
    DatosLista lista = 3;
    DatosMapa mapa = 4;
  }
}

message DatosLista {
  repeated string valores = 1;
}

message DatosMapa {
  map<string, string> atributos = 1;
}